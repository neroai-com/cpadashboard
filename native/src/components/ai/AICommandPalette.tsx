// AICommandPalette - Not applicable on mobile (Cmd+K is desktop-only)
// This is a placeholder that exports nothing for mobile.
// The AI Advisor screen provides the same functionality via navigation.

import { View } from "react-native";

export default function AICommandPalette() {
  // No keyboard shortcuts on mobile — users access AI via the AIOrb or AI Advisor screen
  return null;
}
